#include "main.h"
#include <string.h>
/**
 * _strlen - Output the lenght of string;
 * @s: string to be checker
 * Return: the lenght of string
*/
int _strlen(char *s)
{
	int a;

	a = strlen(s);
	return (a);
}
